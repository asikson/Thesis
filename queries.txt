create index name on cities (city_name)

create index prof on employees (age, gender)

select e.name, e.surname, d.dept_name
from employees e, dept_manager dm, departments d
where e.emp_id = dm.emp_id
and dm.dept_id = d.dept_id
